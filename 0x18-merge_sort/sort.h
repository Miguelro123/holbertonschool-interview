#ifndef H_MERGE_SORT
#define H_MERGE_SORT

/** Libraries */
#include <stdio.h>
#include <stddef.h>
#include <stdlib.h>

/** Prototypes */
void print_array(const int *array, size_t size);
void merge_sort(int *array, size_t size);
#endif
